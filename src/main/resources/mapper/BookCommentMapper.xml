<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fine.datavz.mapper.BookCommentMapper">
    <select id="findAllD">
        SELECT comment FROM doubancomment;
    </select>
    <select id="findAllA">
        SELECT  comment FROM amazoncomment;
    </select>
</mapper>